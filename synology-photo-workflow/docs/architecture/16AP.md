# 16AP – Lokales HuggingFace-Face-Modell absichern (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-06 (HuggingFace-Face, Referenzpools und Eye-Score)  
**Ziel:** HuggingFace-Face-Inferenz lokal härten und testen

## 1. Betroffene Dateien

- `app/model_runtime.py`
- `app/face_backend_huggingface.py`
- `tests/test_model_runtime.py`
- `tests/test_face_backend_huggingface.py`

## 2. Benötigte Funktionen und Klassen

### `app/model_runtime.py`

```python
from pathlib import Path
from dataclasses import dataclass
import hashlib

@dataclass
class ModelRuntimeConfig:
    model_path: Path
    model_hash: str
    provider: str
    preprocessing: dict

class ModelRuntime:
    def __init__(self, config: ModelRuntimeConfig):
        self.config = config

    def validate_model(self) -> bool:
        """Prüft Modellpfad und SHA256-Hash."""
        ...

    def load_model(self) -> object:
        """Lädt Modell einmal pro Prozess."""
        ...
```

### `app/face_backend_huggingface.py`

```python
class HuggingFaceFaceBackend:
    def __init__(self, runtime: "ModelRuntime"):
        self.runtime = runtime

    def detect(self, image_path: Path) -> list["DetectedFace"]:
        """Lokale HuggingFace-Detektion/Embedding-Inferenz."""
        ...
```

## 3. Verwendete Schnittstellen

- `ModelRuntime` (lokal)
- `ModelRuntimeConfig` (lokal)
- `DetectedFace` (aus `app/face_backend.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `validate_model()` | `ModelRuntimeConfig` | `bool` |
| `load_model()` | `ModelRuntimeConfig` | Modell-Objekt |
| `detect()` | `image_path` | `list[DetectedFace]` |

## 5. Relevante Datenmodelle

- `ModelRuntimeConfig`
- `DetectedFace` (siehe `00AP_v3.md` Abschnitt 5.3)

## 6. Relevante Regeln aus v1.1

- **Das Backend lädt nur lokal bereitgestellte Modellgewichte aus `WORKFLOW_DATA/models/` und prüft SHA256.**
- **Es liefert nur flüchtige `DetectedFace`-Objekte mit Confidence, normalisierter Bounding Box und RAM-Embedding.**
- **Das Matching bleibt bei `family_recognition.py`.**
- **Kein Download, keine Netzwerk-Inferenz, keine persistenten Embeddings.**

## 7. Erforderliche Tests

- `tests/test_model_runtime.py`
  - Modellpfad und Hash werden validiert
  - Modell wird einmal pro Prozess geladen
  - Fehlendes Modell wird korrekt behandelt
- `tests/test_face_backend_huggingface.py`
  - HuggingFace-Backend funktioniert
  - Nur flüchtige `DetectedFace`-Objekte werden geliefert
  - Keine persistenten Embeddings

## 8. Akzeptanzkriterien

- [ ] Modellpfad und Hash werden validiert
- [ ] Modell wird einmal pro Prozess geladen
- [ ] Fehlendes Modell wird korrekt behandelt
- [ ] HuggingFace-Backend funktioniert
- [ ] Nur flüchtige `DetectedFace`-Objekte werden geliefert
- [ ] Keine persistenten Embeddings
- [ ] Kein Download, keine Netzwerk-Inferenz
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**