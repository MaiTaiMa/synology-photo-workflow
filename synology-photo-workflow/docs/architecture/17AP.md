# 17AP – PHASE3-Finalisierung und Transfer (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-01 bis AP-16  
**Ziel:** Optionalen, resume-fähigen Transfer eines `phase2_completed`-Batches implementieren

## 1. Betroffene Dateien

- `app/finalization.py`
- `app/result_contract.py`
- `app/configuration.py`
- `app/phases.py`

## 2. Benötigte Funktionen und Klassen

### `app/finalization.py`

```python
from dataclasses import dataclass
from pathlib import Path
from typing import Literal

@dataclass
class FinalizationManifest:
    schema_version: str
    batch_id: str
    created_at: str
    updated_at: str
    producer_version: str
    source_batch_path: str
    target_batch_path: str | None
    publish_enabled: bool
    mode: Literal["copy", "move"] | None
    entries: list["ManifestEntry"]
    config_fingerprint: str
    state: str
    hash: str

@dataclass
class FinalizationResult:
    batch_id: str
    state: str
    target_batch_path: Path | None
    manifest_hash: str | None
    error_reason: str | None

def finalize_batch(
    batch_path: Path,
    config: "Config",
    storage: "Storage",
    state_manager: "StateManager",
) -> FinalizationResult:
    """Orchestriert PHASE3 komplett."""
    ...
```

## 3. Verwendete Schnittstellen

- `FinalizationManifest` (lokal)
- `FinalizationResult` (lokal)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)
- `Storage` (aus `app/storage.py` gemäß `00AP_v3.md`)
- `StateManager` (aus `app/state.py` gemäß `00AP_v3.md`)
- `ManifestEntry` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `finalize_batch()` | `batch_path`, `Config`, `Storage`, `StateManager` | `FinalizationResult` |

## 5. Relevante Datenmodelle

- `FinalizationManifest`
- `FinalizationResult`
- `ManifestEntry` (siehe `00AP_v3.md` Abschnitt 4.3)

## 6. Relevante Regeln aus v1.1

- **Nur `phase2_completed` darf starten.**
- **Deaktivierte Finalisierung/Publish führt keine Dateiaktion aus.**
- **Vor Transfer atomar validiertes Manifest.**
- **`copy` erhält Quelle; `move` ist zwingend `copy → verify → source removal`.**
- **Ziel per Dateiliste, Größe und SHA256 prüfen; danach Indexwartephase und resume-fähige Fortsetzung.**
- **Quelle innerhalb `paths.basedir`, Ziel innerhalb `paths.publish_root`.**
- **Keine ARW-, Archiv-, Review-, Referenzpool- oder Kalibrierungsänderung.**
- **Kein Rücktransfer bei Fehler.**

## 7. Erforderliche Tests

- `tests/test_finalization.py`
  - Nur `phase2_completed` darf starten
  - Deaktivierte Finalisierung/Publish führt keine Dateiaktion aus
  - Copy und Move funktionieren korrekt
  - Teiltransfer, Kollision und Hashabweichung werden korrekt behandelt
  - Index-Timeout wird korrekt behandelt
  - Resume funktioniert ohne Datenverlust

## 8. Akzeptanzkriterien

- [ ] Nur `phase2_completed` darf starten
- [ ] Deaktivierte Finalisierung/Publish führt keine Dateiaktion aus
- [ ] Copy und Move funktionieren korrekt
- [ ] Teiltransfer, Kollision und Hashabweichung werden korrekt behandelt
- [ ] Index-Timeout wird korrekt behandelt
- [ ] Resume funktioniert ohne Datenverlust
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**