# 15AP – Lokales CLIP-Modell absichern (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-05 (Lokales CLIP-Culling und Decision-Composer)  
**Ziel:** Die lokale CLIP-Laufzeit härten und testen

## 1. Betroffene Dateien

- `app/model_runtime.py`
- `app/clip_taste_adapter.py`
- `tests/test_model_runtime.py`
- `tests/test_clip_taste_adapter.py`

## 2. Benötigte Funktionen und Klassen

### `app/model_runtime.py`

```python
from pathlib import Path
from dataclasses import dataclass
import hashlib

@dataclass
class ModelRuntimeConfig:
    model_path: Path
    model_hash: str
    provider: str
    preprocessing: dict

class ModelRuntime:
    def __init__(self, config: ModelRuntimeConfig):
        self.config = config

    def validate_model(self) -> bool:
        """Prüft Modellpfad und SHA256-Hash."""
        ...

    def load_model(self) -> object:
        """Lädt Modell einmal pro Prozess."""
        ...
```

### `app/clip_taste_adapter.py`

```python
class ClipTasteAdapter:
    def __init__(self, runtime: "ModelRuntime"):
        self.runtime = runtime

    def score(self, image_path: Path, config: "Config") -> "TechnicalTasteScore":
        """Lokales CLIP-Culling."""
        ...
```

## 3. Verwendete Schnittstellen

- `ModelRuntime` (lokal)
- `ModelRuntimeConfig` (lokal)
- `TechnicalTasteScore` (aus `app/culling.py` gemäß `00AP_v3.md`)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `validate_model()` | `ModelRuntimeConfig` | `bool` |
| `load_model()` | `ModelRuntimeConfig` | Modell-Objekt |
| `score()` | `image_path`, `Config` | `TechnicalTasteScore` |

## 5. Relevante Datenmodelle

- `ModelRuntimeConfig`
- `TechnicalTasteScore` (siehe `00AP_v3.md` Abschnitt 5.3)

## 6. Relevante Regeln aus v1.1

- **Lokaler Modellpfad und SHA256 werden geprüft.**
- **Modell wird einmal pro Prozess geladen.**
- **Aktive Geschmack-Referenzen/Prompts erzeugen `personal_score`.**
- **Fehlendes/falsches Modell deaktiviert nur den Adapter oder erzeugt einen kontrollierten Fehler; kein Laufzeitdownload und keine Netzwerk-Inferenz.**

## 7. Erforderliche Tests

- `tests/test_model_runtime.py`
  - Modellpfad und Hash werden validiert
  - Modell wird einmal pro Prozess geladen
  - Fehlendes Modell wird korrekt behandelt
- `tests/test_clip_taste_adapter.py`
  - CLIP-Modell wird korrekt geladen
  - `personal_score` wird korrekt berechnet
  - Aktive Geschmack-Referenzen/Prompts werden korrekt verwendet

## 8. Akzeptanzkriterien

- [ ] Modellpfad und Hash werden validiert
- [ ] Modell wird einmal pro Prozess geladen
- [ ] Fehlendes Modell wird korrekt behandelt
- [ ] CLIP-Modell wird korrekt geladen
- [ ] `personal_score` wird korrekt berechnet
- [ ] Aktive Geschmack-Referenzen/Prompts werden korrekt verwendet
- [ ] Kein Laufzeitdownload und keine Netzwerk-Inferenz
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**