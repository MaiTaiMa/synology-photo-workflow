# 08AP – Manual Keep (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-03 (Inventory und Batch-ID), AP-05 (Lokales CLIP-Culling und Decision-Composer)  
**Ziel:** Externe Keep-Bilder eindeutig und unabhängig von Culling zuordnen

## 1. Betroffene Dateien

- `app/manual_keep.py`
- `app/manual_keep_similarity.py`

## 2. Benötigte Funktionen und Klassen

### `app/manual_keep.py`

```python
from dataclasses import dataclass
from pathlib import Path

@dataclass
class ManualKeepResult:
    manual_keep: bool
    manual_keep_match: bool
    matched_path: Path | None
    confidence: float

def match_manual_keep(
    inventory: "InventoryResult",
    manual_keep_dir: Path,
    config: "Config",
) -> list[ManualKeepResult]:
    """Ordnet externe Manual-Keep-Bilder dem Batch zu."""
    ...
```

### `app/manual_keep_similarity.py`

```python
from PIL import Image
import numpy as np

def compare_images(img1_path: str, img2_path: str) -> float:
    """Zweistufiger Vergleich: Vorfilter + Endprüfung."""
    ...

def _prefilter(img1: Image.Image, img2: Image.Image) -> bool:
    """Schneller auflösungsrobuster Vorfilter."""
    ...

def _final_check(img1: Image.Image, img2: Image.Image) -> float:
    """Strenge normalisierte Endprüfung."""
    ...
```

## 3. Verwendete Schnittstellen

- `ManualKeepResult` (lokal)
- `InventoryResult` (aus `app/inventory.py` gemäß `00AP_v3.md`)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `match_manual_keep()` | `InventoryResult`, `manual_keep_dir`, `Config` | `list[ManualKeepResult]` |
| `compare_images()` | `img1_path`, `img2_path` | `float` |

## 5. Relevante Datenmodelle

- `ManualKeepResult`

## 6. Relevante Regeln aus v1.1

- **Zweistufiger Prozess:** schneller auflösungsrobuster Vorfilter, dann strenge normalisierte Endprüfung.
- **`manual_keep` erzwingt `keep` mit Grund `manual_keep_match`.**
- **Quelldatei wird nach `used/` verschoben.**
- **Kein Match bei Mehrdeutigkeit.**

## 7. Erforderliche Tests

- `tests/test_manual_keep.py`
  - Zweistufiger Prozess funktioniert
  - `manual_keep` erzwingt `keep`
  - Quelldatei wird verschoben
  - Ähnlichkeitsberechnung ist korrekt
  - Kein Match bei Mehrdeutigkeit

## 8. Akzeptanzkriterien

- [ ] Zweistufiger Prozess funktioniert korrekt
- [ ] `manual_keep` erzwingt `keep` mit Grund
- [ ] Quelldatei wird nach `used/` verschoben
- [ ] Ähnlichkeitsberechnung ist korrekt
- [ ] Kein Match bei Mehrdeutigkeit
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**