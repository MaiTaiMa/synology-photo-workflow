# 14AP – Container und Betrieb

**Status:** Bereit  
**Abhängigkeiten:** 01AP.md bis 13AP.md  
**Ziel:** Docker- und Shell-Betrieb für lokale Modelle und spätere optionale Synology-Aktivierung

## Kontext-Begrenzung

Lies für dieses Arbeitspaket ausschließlich `00AP.md`, diese Datei sowie die unten unter „Betroffene Dateien" genannten Dateien und deren direkte Importe. Lies keine AP-Dateien außer `00AP.md` und dieser Datei, kein Modul aus `app/` über die in `00AP.md` genannte CLI-Startlogik hinaus, und nicht `docs/spec_v1-1/99_Basic-Photo-Workflow_Spezifikation_v1-1.md`. Bei Unklarheiten konsultiere ausschließlich die passenden Kapitel unter `docs/spec_v1-1/00_...md` bis `07_...md`.

## 1. Betroffene Dateien

- `Dockerfile`
- `docker-compose.yml`
- `scripts/preflight.sh`
- `scripts/run-workflow.sh`

## 2. Benötigte Funktionen und Klassen

### `Dockerfile`

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
COPY config/ ./config/

ENTRYPOINT ["python", "-m", "app.cli"]
```

### `docker-compose.yml`

```yaml
version: "3.8"

services:
  workflow:
    build: .
    volumes:
      - ./config:/config:ro
      - ./NAS_EXAMPLE:/nas:rw
      - ./WORKFLOW_DATA/models:/models:ro
    environment:
      - WORKFLOW_CONFIG=/config/config.yaml
      - WORKFLOW_BASEDIR=/nas
      - WORKFLOW_MODELS=/models
```

### `scripts/preflight.sh`

```bash
#!/bin/bash
# Prüft Mounts und startet den Workflow
```

### `scripts/run-workflow.sh`

```bash
#!/bin/bash
# Startet den Workflow
```

## 3. Verwendete Schnittstellen

- Keine (Shell-Skripte und Docker)

## 4. Input und Output

| Datei | Input | Output |
|---|---|---|
| `Dockerfile` | Build-Kontext | Docker-Image |
| `docker-compose.yml` | Compose-Konfiguration | Laufender Container |
| `scripts/preflight.sh` | Shell-Aufruf | Exit-Code |
| `scripts/run-workflow.sh` | Shell-Aufruf | Exit-Code |

## 5. Relevante Datenmodelle

- Keine

## 6. Relevante Regeln aus v1.1

- **Ein verbindlicher Root-`requirements.txt`.**
- **Modelle und Laufzeitdaten liegen als NAS-Mounts, nicht im Image oder Git.**
- **Shell prüft nur Umgebung und startet `python -m app.cli`.**
- **Secrets werden ausschließlich als Umgebungsvariablen an den Container übergeben und nie in Config/Artefakten abgelegt.**

## 7. Erforderliche Tests

- `tests/test_docker_smoke.py`
  - `docker build` erfolgreich
  - Container startet mit `--help`
  - Mounts für `config.yaml`, NAS-Pfade und Modelle korrekt
  - Secrets-freie Ausgabe

## 8. Akzeptanzkriterien

- [ ] `docker build` erfolgreich
- [ ] Container startet mit `--help`
- [ ] Mounts für `config.yaml`, NAS-Pfade und Modelle korrekt
- [ ] Secrets-freie Ausgabe
- [ ] Keine Geschäftslogik in Shell-Skripten
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**