# 20AP – PHASE3- und Synology-Abnahme (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-17 (PHASE3-Finalisierung und Transfer), AP-18 (Synology-Photos-Adapter und Capability-Gate), AP-19 (Synology-Metadaten und bekannte Personen)  
**Ziel:** PHASE3, Transfer und optionalen Synology-Adapter auf dem konkreten Ziel-NAS abnehmen und releasefähig dokumentieren

## 1. Betroffene Dateien

- `docs/ABNAHMEBERICHT_PHASE3.md`
- `tests/integration/test_phase3_transfer.py`
- `tests/integration/test_synology_people_pilot.py`
- `scripts/preflight_phase3.sh`

## 2. Benötigte Funktionen und Klassen

### `docs/ABNAHMEBERICHT_PHASE3.md`

```markdown
# Abnahmebericht PHASE3

## Zusammenfassung
...

## Erfüllte Spec-Kapitel
...

## Testergebnisse
...

## Bekannte Einschränkungen
...
```

### `scripts/preflight_phase3.sh`

```bash
#!/bin/bash
# Prüft Umgebung und Mounts für PHASE3
```

## 3. Verwendete Schnittstellen

- Keine (Dokumentation und Shell)

## 4. Input und Output

| Datei | Input | Output |
|---|---|---|
| `docs/ABNAHMEBERICHT_PHASE3.md` | Testergebnisse, Spec-Erfüllung | Markdown-Dokument |
| `scripts/preflight_phase3.sh` | Shell-Aufruf | Exit-Code |

## 5. Relevante Datenmodelle

- Keine

## 6. Relevante Regeln aus v1.1

- **`finalization.enabled: false` führt keine Datei- und keine API-Aktion aus.**
- **Publish-disabled erzeugt nur zulässige Plan-/Reportartefakte.**
- **Copy und Move befolgen Hashprüfung; Move ist immer `copy → verify → source removal`.**
- **Teiltransfer, Kollision und Neustart sind resume-fähig.**
- **Index-Timeout führt zu keiner Dateiaktion.**
- **Capability-Gate blockiert nicht nachgewiesene API-Funktionen.**
- **Rating, Tags, Beschreibung und Personen-Zuordnung werden nur nach erfolgreichem Ziel-NAS-Pilot und Rückleseprüfung als Erfolg markiert.**
- **API-Fehler verändern weder Archive noch ARWs noch PHASE2-Status.**
- **Secrets, Bildbytes, Face-Crops, Embeddings und Referenzbilder erscheinen in keinem Log, State, Manifest, CSV oder Report.**

## 7. Erforderliche Tests

- `tests/integration/test_phase3_transfer.py`
  - `finalization.enabled: false` führt keine Datei- und keine API-Aktion aus
  - Publish-disabled erzeugt nur zulässige Plan-/Reportartefakte
  - Copy und Move befolgen Hashprüfung
  - Teiltransfer, Kollision und Neustart sind resume-fähig
  - Index-Timeout führt zu keiner Dateiaktion
- `tests/integration/test_synology_people_pilot.py`
  - Capability-Gate blockiert nicht nachgewiesene API-Funktionen
  - Rating, Tags, Beschreibung und Personen-Zuordnung werden nur nach erfolgreichem Ziel-NAS-Pilot und Rückleseprüfung als Erfolg markiert
  - API-Fehler verändern weder Archive noch ARWs noch PHASE2-Status
  - Secrets, Bildbytes, Face-Crops, Embeddings und Referenzbilder erscheinen in keinem Log, State, Manifest, CSV oder Report

## 8. Akzeptanzkriterien

- [ ] Alle Unit-, Vertrags-, Resume- und Ziel-NAS-Integrationstests sind dokumentiert erfolgreich
- [ ] Der Bericht nennt NAS-/DSM-/Synology-Photos-Version, Space, nachgewiesene und nicht unterstützte Capabilities sowie bekannte Einschränkungen
- [ ] Ein nicht unterstützter Personen-Write bleibt ein kontrollierter, nicht destruktiver Teilfehler; V1/PHASE2 bleibt vollständig gültig
- [ ] Erst nach expliziter Freigabe dürfen PHASE3 und Synology-Funktionen produktiv aktiviert werden
- [ ] Alle Tests bestehen

---

**Aufgabe vollständig abgeschlossen.**

Alle Arbeitspakete (`01AP_v3_final.md` bis `20AP_v3_final.md`) sowie das zentrale Architektur-Dokument (`00AP_v3.md`) sind erstellt und stehen zum Download bereit.