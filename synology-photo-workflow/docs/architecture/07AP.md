# 07AP – Serienerkennung

**Status:** Bereit  
**Abhängigkeiten:** 05AP.md (Lokales CLIP-Culling und Decision-Composer)  
**Ziel:** Serien aus `ImageAnalysis` gruppieren und als Eingabe für den Composer liefern

## Kontext-Begrenzung

Lies für dieses Arbeitspaket ausschließlich `00AP.md`, diese Datei, `05AP.md` sowie die unten unter „Betroffene Dateien" genannten Dateien und deren direkte Importe. Lies keine weiteren AP-Dateien, kein anderes Modul aus `app/` und nicht `docs/spec_v1-1/99_Basic-Photo-Workflow_Spezifikation_v1-1.md`. Bei Unklarheiten konsultiere ausschließlich die passenden Kapitel unter `docs/spec_v1-1/00_...md` bis `07_...md`.

## 1. Betroffene Dateien

- `app/series_culling.py`

## 2. Benötigte Funktionen und Klassen

```python
from dataclasses import dataclass
from typing import Literal
import numpy as np

@dataclass
class SeriesGroup:
    series_id: str
    images: list["ImageAnalysis"]
    best_index: int

@dataclass
class SeriesResult:
    series_id: str
    series_rank: int
    series_best: bool
    distance_to_best: float

def group_series(
    analyses: list["ImageAnalysis"],
    config: "Config",
) -> list[SeriesGroup]:
    """Gruppiert ähnliche Bilder zu Serien."""
    ...

def rank_series(group: SeriesGroup) -> list[SeriesResult]:
    """Rankt Bilder innerhalb einer Serie."""
    ...

def compute_similarity(
    img1: "ImageAnalysis",
    img2: "ImageAnalysis",
) -> float:
    """Berechnet Ähnlichkeit zwischen zwei Bildern."""
    ...
```

## 3. Verwendete Schnittstellen

- `SeriesGroup` (lokal)
- `SeriesResult` (lokal)
- `ImageAnalysis` (aus `result_contract.py` gemäß `00AP.md`)
- `Config` (aus `result_contract.py` gemäß `00AP.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `group_series()` | `list[ImageAnalysis]`, `Config` | `list[SeriesGroup]` |
| `rank_series()` | `SeriesGroup` | `list[SeriesResult]` |
| `compute_similarity()` | `ImageAnalysis`, `ImageAnalysis` | `float` |

## 5. Relevante Datenmodelle

- `SeriesGroup`
- `SeriesResult`
- `ImageAnalysis` (siehe `00AP.md` Abschnitt 4.2)

## 6. Relevante Regeln aus v1.1

- **`series_id` ist eine eindeutige Zeichenkette pro Serie innerhalb eines Batches.**
- **`series_rank` ist 1-basiert (1 = bestes Bild).**
- **`series_best` ist ein boolescher Wert.**
- **Das Bestbild darf höchstens um eine Klasse aufgewertet werden.**
- **Andere Bilder dürfen nur mit dokumentierter Distanz zum Bestbild abgewertet werden.**
- **Das Modul setzt keine finale Entscheidung, schreibt keine Metadaten und verändert keine Dateien.**

## 7. Erforderliche Tests

- `tests/test_series_culling.py`
  - Serien werden korrekt gruppiert
  - `series_id`, `series_rank`, `series_best` sind korrekt
  - Bestbild wird korrekt identifiziert
  - Ähnlichkeitsberechnung ist korrekt
  - Keine finale Entscheidung, keine Metadaten, keine Dateiänderung

## 8. Akzeptanzkriterien

- [ ] Serien werden korrekt gruppiert
- [ ] `series_id`, `series_rank`, `series_best` sind korrekt
- [ ] Bestbild wird korrekt identifiziert
- [ ] Ähnlichkeitsberechnung ist korrekt
- [ ] Keine finale Entscheidung, keine Metadaten, keine Dateiänderung
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**