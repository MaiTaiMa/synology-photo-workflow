# 11AP – PHASE1 und WorkUnits (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-01 bis AP-10  
**Ziel:** PHASE1 mit großen Batches und Resume sicher orchestrieren

## 1. Betroffene Dateien

- `app/work_units.py`
- `app/phases.py` (PHASE1-Teil)

## 2. Benötigte Funktionen und Klassen

### `app/work_units.py`

```python
from dataclasses import dataclass
from pathlib import Path
from typing import Literal

@dataclass
class WorkUnitState:
    workunit_id: str
    batch_id: str
    image_range: tuple[int, int]
    state: Literal["pending", "in_progress", "completed", "failed", "paused"]
    timestamp: str
    hash: str
    error_reason: str | None = None

def plan_work_units(
    inventory: "InventoryResult",
    config: "Config",
) -> list[WorkUnitState]:
    """Plant WorkUnits für große Batches."""
    ...
```

### `app/phases.py`

```python
from dataclasses import dataclass
from pathlib import Path

@dataclass
class Phase1Result:
    success: bool
    batch_id: str
    image_results: list["ImageResult"]
    manifest_path: Path
    csv_path: Path
    error_reason: str | None

def run_phase1(
    batch_path: Path,
    config: "Config",
    storage: "Storage",
    state_manager: "StateManager",
) -> Phase1Result:
    """Orchestriert PHASE1 komplett."""
    ...
```

## 3. Verwendete Schnittstellen

- `WorkUnitState` (lokal)
- `Phase1Result` (lokal)
- `InventoryResult` (aus `app/inventory.py` gemäß `00AP_v3.md`)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)
- `Storage` (aus `app/storage.py` gemäß `00AP_v3.md`)
- `StateManager` (aus `app/state.py` gemäß `00AP_v3.md`)
- `ImageResult` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `plan_work_units()` | `InventoryResult`, `Config` | `list[WorkUnitState]` |
| `run_phase1()` | `batch_path`, `Config`, `Storage`, `StateManager` | `Phase1Result` |

## 5. Relevante Datenmodelle

- `WorkUnitState`
- `Phase1Result`
- `ImageResult` (siehe `00AP_v3.md` Abschnitt 4.2)

## 6. Relevante Regeln aus v1.1

- **States `phase1_started → phase1_moving → phase1_completed`.**
- **`phase1_moving` vor jedem sichtbaren Dateimove.**
- **Kein stilles Scoring.**
- **Der physische Batch wird erst nach Abschluss aller WorkUnits atomar nach `02_TEMP_IMAGES` übergeben.**
- **`SAVE/culling_scores.csv` und Phase-1-Manifest müssen vorhanden sein.**

## 7. Erforderliche Tests

- `tests/test_phase1_integration.py`
  - Alle 8 Schritte aus Spec 3.2 werden durchlaufen
  - Zustandsautomat funktioniert
  - Atomare Übergabe funktioniert
  - Manifest und CSV werden erstellt
  - `source_batch` und `imagecount` funktionieren
  - Resume funktioniert

## 8. Akzeptanzkriterien

- [ ] Alle 8 Schritte aus Spec 3.2 werden durchlaufen
- [ ] Zustandsautomat funktioniert korrekt
- [ ] Atomare Übergabe nach `02_TEMP_IMAGES` funktioniert
- [ ] `SAVE/culling_scores.csv` und Phase-1-Manifest werden erstellt
- [ ] `source_batch` und `imagecount` funktionieren
- [ ] Resume funktioniert
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**