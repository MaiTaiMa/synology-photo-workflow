# 19AP – Synology-Metadaten und bekannte Personen (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-17 (PHASE3-Finalisierung und Transfer), AP-18 (Synology-Photos-Adapter und Capability-Gate)  
**Ziel:** Bereits lokal vorhandene Ratings, erlaubte Tags, optionale Beschreibung und vorrangig bekannte Personen kontrolliert in Synology Photos abbilden

## 1. Betroffene Dateien

- `app/synology_photos_adapter.py`
- `app/finalization.py`
- `app/result_contract.py`
- `tests/test_synology_metadata_mapping.py`
- `tests/integration/test_synology_people_pilot.py`

## 2. Benötigte Funktionen und Klassen

### `app/synology_photos_adapter.py`

```python
class SynologyPhotosAdapter:
    def __init__(self, config: "Config"):
        self.config = config

    def healthcheck(self) -> "ApiCapabilityReport":
        """Prüft API-Discovery, Authentisierung, Space- und Item-Auflösung."""
        ...

    def resolve_item(self, relative_path: str, space: str) -> "ResolvedPhotoItem":
        """Löst Zielbild als Synology-Photos-Item auf."""
        ...

    def get_metadata(self, item_id: str) -> "PublishedMetadata":
        """Liest Metadaten eines Items."""
        ...

    def set_rating(self, item_id: str, rating: int) -> "ApiWriteResult":
        """Setzt Rating eines Items."""
        ...

    def ensure_tags(self, item_id: str, tags: list[str]) -> "ApiWriteResult":
        """Setzt Tags eines Items."""
        ...

    def set_description(self, item_id: str, description: str) -> "ApiWriteResult":
        """Setzt Beschreibung eines Items."""
        ...

    def resolve_existing_person(self, slug: str, space: str) -> "ResolvedPerson":
        """Löst bestehende Synology-Person auf."""
        ...

    def assign_existing_person(
        self,
        item_id: str,
        person_id: str,
        bounding_box: "NormalizedBoundingBox",
    ) -> "ApiWriteResult":
        """Weist bestehende Synology-Person einem Item zu."""
        ...
```

## 3. Verwendete Schnittstellen

- `SynologyPhotosAdapter` (lokal)
- `ApiCapabilityReport` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `ResolvedPhotoItem` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `PublishedMetadata` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `ApiWriteResult` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `ResolvedPerson` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `NormalizedBoundingBox` (aus `app/synology_photos_adapter.py` gemäß `00AP_v3.md`)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `healthcheck()` | - | `ApiCapabilityReport` |
| `resolve_item()` | `relative_path`, `space` | `ResolvedPhotoItem` |
| `get_metadata()` | `item_id` | `PublishedMetadata` |
| `set_rating()` | `item_id`, `rating` | `ApiWriteResult` |
| `ensure_tags()` | `item_id`, `tags` | `ApiWriteResult` |
| `set_description()` | `item_id`, `description` | `ApiWriteResult` |
| `resolve_existing_person()` | `slug`, `space` | `ResolvedPerson` |
| `assign_existing_person()` | `item_id`, `person_id`, `bounding_box` | `ApiWriteResult` |

## 5. Relevante Datenmodelle

- `ApiCapabilityReport`
- `ResolvedPhotoItem`
- `PublishedMetadata`
- `ApiWriteResult`
- `ResolvedPerson`
- `NormalizedBoundingBox`

## 6. Relevante Regeln aus v1.1

- **Eine echte Personen-Zuordnung ist nur zulässig, wenn ein lokaler eindeutiger bekannter Face-Match, eine bestehende eindeutige Synology-Person, eine validierte normalisierte Bounding Box, eindeutiges Zielbild und Rückleseprüfung vorliegen.**
- **Unbekannte Gesichter/Personen werden nie übertragen.**
- **Personenerzeugung, Merge, Rename und Delete sind nicht Teil dieses AP.**
- **Bereits korrekt gesetzte Werte sind Erfolg.**
- **Mehrdeutige Items/Personen, fehlende Geometrie oder Rückleseabweichung führen zu keinem Personen-Schreiben.**
- **API-Fehler erzeugen `phase3_api_metadata_partial` oder `phase3_api_metadata_failed`; sie lösen niemals Dateilöschung, Rücktransfer oder PHASE2-Rollback aus.**

## 7. Erforderliche Tests

- `tests/test_synology_metadata_mapping.py`
  - Mapping, Tag-Deduplizierung und Readback mit Test-Doubles
- `tests/integration/test_synology_people_pilot.py`
  - Echter Ziel-NAS-Pilot: bestehende Testperson, eindeutiges Testbild, Zuordnung und Rücklese
  - Bei Capability- oder Geometriefehler nur sicherer Tag-Fallback, sofern erlaubt
  - Keine Face-Crops, Embeddings, Bildbytes, Secrets oder lokalen Pfade in API-Payloads/Artefakten

## 8. Akzeptanzkriterien

- [ ] Mapping, Tag-Deduplizierung und Readback funktionieren mit Test-Doubles
- [ ] Echter Ziel-NAS-Pilot: bestehende Testperson, eindeutiges Testbild, Zuordnung und Rücklese
- [ ] Bei Capability- oder Geometriefehler nur sicherer Tag-Fallback, sofern erlaubt
- [ ] Keine Face-Crops, Embeddings, Bildbytes, Secrets oder lokalen Pfade in API-Payloads/Artefakten
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**