# 18AP – Synology-Photos-Adapter und Capability-Gate (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-17 (PHASE3-Finalisierung und Transfer)  
**Ziel:** Gekapselten, capability-gesteuerten Synology-Photos-Adapter implementieren

## 1. Betroffene Dateien

- `app/synology_photos_adapter.py`
- `app/configuration.py`
- `app/result_contract.py`
- `tests/test_synology_adapter_capabilities.py`

## 2. Benötigte Funktionen und Klassen

### `app/synology_photos_adapter.py`

```python
from dataclasses import dataclass
from typing import Protocol
from pathlib import Path

@dataclass
class ApiCapabilityReport:
    auth_success: bool
    api_discovery_success: bool
    space_accessible: bool
    item_resolution_success: bool
    rating_write_supported: bool
    tags_write_supported: bool
    description_write_supported: bool
    person_write_supported: bool

@dataclass
class ResolvedPhotoItem:
    item_id: str
    relative_path: str
    space: str

@dataclass
class PublishedMetadata:
    rating: int
    tags: list[str]
    description: str | None

@dataclass
class ApiWriteResult:
    success: bool
    error_reason: str | None

class SynologyPhotosAdapterProtocol(Protocol):
    def healthcheck(self) -> ApiCapabilityReport: ...
    def resolve_item(self, relative_path: str, space: str) -> ResolvedPhotoItem: ...
    def get_metadata(self, item_id: str) -> PublishedMetadata: ...
    def set_rating(self, item_id: str, rating: int) -> ApiWriteResult: ...
    def ensure_tags(self, item_id: str, tags: list[str]) -> ApiWriteResult: ...
    def set_description(self, item_id: str, description: str) -> ApiWriteResult: ...
    def resolve_existing_person(self, slug: str, space: str) -> ResolvedPerson: ...
    def assign_existing_person(
        self,
        item_id: str,
        person_id: str,
        bounding_box: NormalizedBoundingBox,
    ) -> ApiWriteResult: ...
```

## 3. Verwendete Schnittstellen

- `SynologyPhotosAdapterProtocol` (Protocol, aus `00AP_v3.md`)
- `ApiCapabilityReport` (lokal)
- `ResolvedPhotoItem` (lokal)
- `PublishedMetadata` (lokal)
- `ApiWriteResult` (lokal)
- `Config` (aus `result_contract.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `healthcheck()` | - | `ApiCapabilityReport` |
| `resolve_item()` | `relative_path`, `space` | `ResolvedPhotoItem` |
| `get_metadata()` | `item_id` | `PublishedMetadata` |
| `set_rating()` | `item_id`, `rating` | `ApiWriteResult` |
| `ensure_tags()` | `item_id`, `tags` | `ApiWriteResult` |
| `set_description()` | `item_id`, `description` | `ApiWriteResult` |
| `resolve_existing_person()` | `slug`, `space` | `ResolvedPerson` |
| `assign_existing_person()` | `item_id`, `person_id`, `bounding_box` | `ApiWriteResult` |

## 5. Relevante Datenmodelle

- `ApiCapabilityReport`
- `ResolvedPhotoItem`
- `PublishedMetadata`
- `ApiWriteResult`

## 6. Relevante Regeln aus v1.1

- **Keine konkrete API-Schreibfähigkeit wird vor Ziel-NAS-Pilotlauf angenommen.**
- **Nicht nachgewiesene Fähigkeiten gelten als `unsupported`.**
- **Keine Bildbytes, Crops, Embeddings oder Referenzbilder werden übertragen.**
- **Personal und Shared Space sind getrennt capability-zuprüfen.**
- **Nicht dokumentierte/private Endpunkte dürfen nicht geraten oder erzwungen werden.**

## 7. Erforderliche Tests

- `tests/test_synology_adapter_capabilities.py`
  - Discovery, Authentisierung, Space- und Item-Auflösung mit Test-Doubles
  - Fehlende Fähigkeit führt zu `capability_unsupported`, nicht zu einem Workaround
  - Secrets erscheinen in keinem Artefakt
  - Personal und Shared Space sind getrennt capability-zuprüfen

## 8. Akzeptanzkriterien

- [ ] Discovery, Authentisierung, Space- und Item-Auflösung funktionieren mit Test-Doubles
- [ ] Fehlende Fähigkeit führt zu `capability_unsupported`, nicht zu einem Workaround
- [ ] Secrets erscheinen in keinem Artefakt
- [ ] Personal und Shared Space sind getrennt capability-zuprüfen
- [ ] Ein echter Ziel-NAS-Pilot ist Voraussetzung für Freigabe schreibender Methoden
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**