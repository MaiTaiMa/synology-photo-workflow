# 13AP – Reporting und Run-Summary (V3 final)

**Status:** Bereit  
**Abhängigkeiten:** AP-11 (PHASE1 und WorkUnits), AP-12 (PHASE2, Review und Kalibrierung)  
**Ziel:** Jeder Lauf ist nachvollziehbar und handlungsfähig dokumentiert

## 1. Betroffene Dateien

- `app/reporting.py`

## 2. Benötigte Funktionen und Klassen

```python
from dataclasses import dataclass
from pathlib import Path
import json
from typing import Literal

@dataclass
class RunSummary:
    batch_id: str
    phase: str
    status: str
    image_count: int
    keep_count: int
    review_count: int
    reject_count: int
    user_actions_required: list[str]
    error_count: int
    transfer_status: str | None = None
    api_status: str | None = None

def generate_summary(
    phase1_result: "Phase1Result | None",
    phase2_result: "Phase2Result | None",
    phase3_result: "FinalizationResult | None" = None,
    batch_id: str,
) -> RunSummary:
    """Generiert die Run-Summary."""
    ...

def log_error(error: str, context: dict) -> None:
    """Loggt einen Fehler."""
    ...
```

## 3. Verwendete Schnittstellen

- `RunSummary` (lokal)
- `Phase1Result` (aus `app/phases.py` gemäß `00AP_v3.md`)
- `Phase2Result` (aus `app/phases.py` gemäß `00AP_v3.md`)
- `FinalizationResult` (aus `app/finalization.py` gemäß `00AP_v3.md`)

## 4. Input und Output

| Funktion | Input | Output |
|---|---|---|
| `generate_summary()` | `Phase1Result`, `Phase2Result`, `FinalizationResult`, `batch_id` | `RunSummary` |
| `log_error()` | `error`, `context` | `None` |

## 5. Relevante Datenmodelle

- `RunSummary`

## 6. Relevante Regeln aus v1.1

- **JSON-Run-Summary enthält alle Pflichtfelder.**
- **`user_actions_required` bei `max_new`, `max_active`, `review_state_invalid`.**
- **Keine Secrets in Logs.**
- **Keine Bilddaten, Face-Crops, Embeddings oder Tokens in Logs/Reports.**

## 7. Erforderliche Tests

- `tests/test_reporting.py`
  - Run-Summary enthält alle Pflichtfelder
  - `user_actions_required` wird korrekt gesetzt
  - Keine Secrets in Logs
  - Transfer- und API-Status werden korrekt dokumentiert

## 8. Akzeptanzkriterien

- [ ] Run-Summary enthält alle Pflichtfelder
- [ ] `user_actions_required` wird korrekt gesetzt
- [ ] Keine Secrets in Logs
- [ ] Transfer- und API-Status werden korrekt dokumentiert
- [ ] Alle Tests bestehen

---

**Soll ich mit der Überarbeitung der übrigen APs fortfahren?**