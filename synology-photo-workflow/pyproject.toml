# Synology Photo Workflow | pyproject.toml
# Mitentwickler: MaiTai | Erstellt: 2026-07-29 | Projektversion: 7.8.0
# Funktion: Python-, Pytest- und Ruff-Konfiguration.
[project]
name = "synology-photo-workflow"
version = "7.8.0"
requires-python = ">=3.11"
[tool.pytest.ini_options]
testpaths = ["tests"]
[tool.ruff]
line-length = 100
